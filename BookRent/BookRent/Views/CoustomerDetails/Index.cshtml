@model IEnumerable<BookRent.Models.CoustomerDetail>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_BaseLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Phone)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BooksTaken)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Address.House)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Phone)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BooksTaken)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Address.House)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CoustomerId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CoustomerId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CoustomerId })
        </td>
    </tr>
}

</table>
